{
  "name": "pace-forecast",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Adjust your running goal pace for temperature, humidity, and altitude.",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/generate-pages.mjs && tsc -b && vite build",
    "generate": "node scripts/generate-pages.mjs",
    "preview": "vite preview",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
